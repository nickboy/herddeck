{
  "name": "herddeck",
  "private": true,
  "version": "0.0.1",
  "description": "Stream Deck plugin + Bun daemon controlling local and remote herdr agent sessions",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "lint": "biome ci .",
    "lint:fix": "biome check --write .",
    "verify": "bun run lint && bunx tsc --noEmit && bun test && bun run lint:sh && bun run lint:md",
    "lint:sh": "git ls-files '*.sh' | xargs shellcheck -x",
    "lint:md": "npx --yes markdownlint-cli $(git ls-files '*.md')"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
